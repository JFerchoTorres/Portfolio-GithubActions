name: Test

on: [push]

jobs:
    test-build:
        runs-on: ubuntu-latest

        steps:
        - name: ls
          run: |
            echo "HolA Mundo" >> hola.txt
            apg-get install nginx curl